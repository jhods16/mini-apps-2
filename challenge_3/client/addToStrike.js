const addToStrike = (strikes, pins) => {
  // add rollScore first strike on the queue
    // if strike.length === 2
    //  sum scores + 10
    //  remove strike from queue
    //  return sum to the score of the strike's frame (if < 30, if > 30 return 30)
    // else return nothing or some falsey value?
}