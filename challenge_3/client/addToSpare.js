const addToSpare = (spares, pina) => {
  // add rollScore first spare on the queue
    // if spare.length === 1
    //  sum score + 10
    //  remove spare from queue
    //  return sum to the score of the spare's frame (if < 30, if > 30 return 30)
    // else return nothing or some falsey value?
}
